spring:
  application:
    name: gare-routiere

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:gare_routiere}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false

    logging:
      level:
        org.hibernate.SQL: OFF
        org.hibernate.orm.jdbc.bind: OFF

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: othmane.alami2211@gmail.com
    password: lirt tqrl xaho ejmo

    properties:
      mail:
        smtp:
          auth: true

          starttls:
            enable: true

app:
  qr:
    secret: ${JWT_SECRET:mahta_gare_routiere_super_secret_jwt_key_2026_minimum_32_chars}

  jwt:
    secret: ${JWT_SECRET:CHANGE_ME_SECRET_KEY}

    expiration-ms: 86400000
    refresh-expiration-ms: 604800000

  frontend-url: ${FRONTEND_URL:http://localhost:3000}